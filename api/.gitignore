# EcoLife API — local secrets (never commit)
.env
.env.local
.env.*.local

# Uploaded category images (local disk)
uploads/*
!uploads/.gitkeep

# Build / deps
node_modules/
dist/
coverage/

# Prisma
prisma/*.db
prisma/*.db-journal

# Logs / OS
*.log
.DS_Store
