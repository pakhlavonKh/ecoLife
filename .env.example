# Public site (Vite). Leave empty in local dev to use the Vite proxy → Nest :3000.
# VITE_API_URL=https://api.example.com
VITE_API_URL=

# Comma-separated payment providers shown in the booking modal.
# Dev default (when unset): mock,payme,click
# Prod example: payme,click
# VITE_PAYMENT_PROVIDERS=mock,payme,click
